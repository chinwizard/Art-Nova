if (global.currentcocktail = argument0)
    {
    draw_text(self.x, self.y, +string(argument0) + " baby!")
    }
if (global.currentcocktail = argument1)
    {
    draw_text(self.x, self.y, "That's a shitty " +string(argument1)+ " mang...")
    }
